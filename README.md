# Analysis Sales Data with Pandas from Python 

- ไฟล์ sample-store.csv เป็น DataSet
- ไฟล์ Pandas Foundation Final Project.ipynb เป็น Jupyter Notebook ในการทำ Analysis ครั้งนี้
- ไฟล์ data_california.csv เป็นไฟล์ผลลัพธ์ที่ได้จาก Section ข้อ 3
- ไฟล์ clean_state_date.csv เป็นไฟล์ผลลัพธ์ที่ได้จาก Section ข้อ 4

### ประกอบไปด้วย 2 ส่วน ใน Jupyter Notebook

## Section 1 
การทำ EDA สำรวจข้อมูลเบื้องต้น และมีการปรับ Format ให้เป็น Date โดยจะมี 2 cols ได้แก่ ["Order Date", "Ship Date"]
และเปลี่ยนชื่อของ Columns ที่มีการเว้นว่างเช่น ["Order ID"] ให้เป็น ["Order_ID"] กับแปลง Format ให้เป็น lower จะได้ผลลัพธ์เป็น ["order_id"] 

## Section 2
ส่วนของการทำ Data Analysis Part จะทำการตั้งคำถามทั้งหมด 10 ข้อ + 1(Bonus) แล้วทำการวิเคราะห์โดยใช้ Library Pandas จากภาษา Python

